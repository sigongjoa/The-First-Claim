# PROJECT OVERRIDE - React 웹 게임 설정 가이드

이 가이드를 따라 PROJECT OVERRIDE 게임을 React 웹 애플리케이션으로 실행할 수 있습니다.

## 📋 전제 조건

- **Node.js**: v16 이상
- **npm**: v7 이상 또는 **yarn** v1.22 이상
- **Web Browser**: Chrome, Firefox, Safari, Edge (최신 버전)

## 🚀 설치 및 실행 방법

### 1단계: Node.js 설치 확인

```bash
node --version  # v16.x.x 이상
npm --version   # v7.x.x 이상
```

최신 버전을 다운로드하려면: https://nodejs.org/

### 2단계: 웹 프로젝트 폴더로 이동

```bash
cd The-First-Claim/web
```

### 3단계: 의존성 설치

```bash
npm install
```

또는 yarn을 사용하는 경우:

```bash
yarn install
```

> ⏱️ 설치에는 2-5분 정도 소요될 수 있습니다.

### 4단계: 개발 서버 시작

```bash
npm start
```

또는 yarn:

```bash
yarn start
```

자동으로 브라우저가 열리고 http://localhost:3000 으로 이동합니다.

> 만약 브라우저가 자동으로 열리지 않으면, 주소창에 http://localhost:3000 을 입력하세요.

## 🎮 게임 사용 방법

### 1. 환영 화면
- 플레이어 이름을 입력합니다
- 난이도를 선택합니다 (EASY, NORMAL, HARD)
- "게임 시작" 버튼을 클릭합니다

### 2. 게임 화면
- 청구항을 입력합니다 (20자 이상)
- 필요한 개수만큼 청구항을 작성합니다
- "제출" 버튼으로 제출합니다
- 타이머가 0이 되면 자동으로 제출됩니다

### 3. 결과 화면
- 성공/실패 여부를 확인합니다
- 작성한 청구항을 복습합니다
- "다음 레벨" 또는 "다시 하기"를 선택합니다

## 📊 레벨 설명

### Level 1: 기본 청구항 작성 🟢 EASY
```
필요한 청구항: 1개
시간 제한: 300초 (5분)
목표: 간단한 독립항을 작성하세요

예시:
배터리 장치는 양극, 음극, 전해질을 포함한다
```

### Level 2: 종속항 작성 🟡 NORMAL
```
필요한 청구항: 3개
시간 제한: 600초 (10분)
목표: 독립항을 기반으로 종속항을 작성하세요

예시:
청구항 1) 배터리 장치는 양극, 음극, 전해질을 포함한다
청구항 2) 제1항의 배터리에서 양극은 리튬을 포함한다
청구항 3) 제1항의 배터리에서 음극은 흑연을 포함한다
```

### Level 3: 복합 청구항 세트 🔴 HARD
```
필요한 청구항: 5개
시간 제한: 900초 (15분)
목표: 여러 독립항과 종속항을 포함한 청구항 세트를 작성하세요
```

## ✅ 검증 규칙

게임이 다음 규칙으로 청구항을 검증합니다:

- **최소 길이**: 20자 이상
- **기술적 특징**: 포함, 구성, 방법, 단계, 특징, 요소 등 기술 용어 필수
- **명확성**: 모호한 표현 피하기 (등, 같은, 대략, 약, etc.)
- **종속항 참조**: "제X항"으로 선행항 올바르게 참조

## 🛠️ 개발 모드

### 자동 새로고침
파일을 수정하면 브라우저가 자동으로 새로고침됩니다.

### 개발자 도구
```
Windows/Linux: F12 또는 Ctrl+Shift+I
macOS: Cmd+Option+I
```

## 🏗️ 프로덕션 빌드

웹사이트에 배포할 최적화된 빌드를 생성합니다:

```bash
npm run build
```

또는 yarn:

```bash
yarn build
```

> 빌드 결과는 `web/build` 폴더에 생성됩니다.

## 📁 프로젝트 구조

```
web/
├── public/
│   └── index.html              # HTML 엔트리 포인트
├── src/
│   ├── components/
│   │   ├── WelcomeScreen.jsx   # 시작 화면
│   │   ├── GameScreen.jsx      # 게임 플레이
│   │   └── ResultScreen.jsx    # 결과 화면
│   ├── styles/
│   │   ├── WelcomeScreen.css   # 시작 화면 스타일
│   │   ├── GameScreen.css      # 게임 스타일
│   │   └── ResultScreen.css    # 결과 스타일
│   ├── App.jsx                 # 메인 앱
│   ├── index.js                # 시작점
│   └── index.css               # 전역 스타일
├── package.json                # 프로젝트 설정
└── README.md                   # 프로젝트 설명
```

## 🎨 커스터마이징

### 색상 변경
`src/styles/*.css` 파일에서 다음 색상 값을 변경합니다:
- 주색상: `#667eea`, `#764ba2`
- 성공: `#2ecc71`
- 경고: `#f39c12`
- 오류: `#e74c3c`

### 폰트 변경
`web/public/index.html`의 `<head>` 섹션에서 Google Fonts를 변경합니다:

```html
<link href="https://fonts.googleapis.com/css2?family=YOUR_FONT:wght@400;700&display=swap" rel="stylesheet">
```

### 레벨 추가/수정
`src/components/GameScreen.jsx`의 `levelConfigs` 객체를 수정합니다.

## 🐛 문제 해결

### npm: command not found
Node.js를 설치해주세요: https://nodejs.org/

### Port 3000 already in use
```bash
# 다른 포트 사용
PORT=3001 npm start
```

### 페이지가 로드되지 않음
```bash
# 캐시 삭제 후 재설치
rm -rf node_modules package-lock.json
npm install
npm start
```

### 성능 문제
```bash
# 개발자 도구에서 "React DevTools" 확장 비활성화
```

## 📚 관련 문서

- [React 공식 문서](https://react.dev/)
- [Create React App 문서](https://create-react-app.dev/)
- [JavaScript 기초](https://developer.mozilla.org/en-US/docs/Web/JavaScript/)

## 🚀 다음 단계

1. **백엔드 통합**: Python FastAPI로 API 서버 구성
2. **데이터베이스**: 플레이어 진행 상황 저장
3. **배포**: Vercel, Netlify, GitHub Pages 등으로 배포
4. **모바일**: React Native로 모바일 앱 개발

## 📝 라이선스

PROJECT OVERRIDE - Test-Driven Legal Engine

---

**마지막 업데이트**: 2025년 12월 3일
**React 버전**: 18.2.0
